{% for block in section.blocks %}
  {% if section.settings.custom_image %}
    <img src="{{ section.settings.imageno | img_url: 'large' }}" alt="Custom Image">
  {% endif %}
  <button>
    <p>{{ block.settings.textoB }}</p>
  </button>

{% endfor %}


{% schema %}
  {
    "name": "Bienvenida",
    "class": "inicioPag",
    "tag": "section",
    "settings": [
      {
        "type": "image_picker",
        "id": "imageno",
        "label": "Logo"
      }, {
        "type": "range",
        "id": "logo_width",
        "label": "Logo width",
        "default": 12,
        "min": 8,
        "max": 64,
        "step": 1
      }
    ],
    "presets": [
      {
        "name": "GRID BUTTN-IMGS"
      }
    ],
    "blocks": [
      {
        "name": "BotonImg",
        "type": "ui",
        "settings": [
          {
            "type": "text",
            "id": "textoB",
            "label": "Texto Button"
          }
        ]
      }
    ]
  }
{% endschema %}